<p>{{# _t }}Use images that are at least 1200×630 pixels for the best display on high resolution devices. At the minimum, you should use images that are 600×315 pixels to display with larger images.{{/ _t }}</p>
<p>{{# _t }}The minimum image size is 200×200 pixels. If you try to use an image smaller than this you will see an error in the <a target="_blank" href="https://developers.facebook.com/tools/debug">URL Debugger</a>.{{/ _t }}</p>
